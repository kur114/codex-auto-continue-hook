{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "decision": {
      "type": "string",
      "enum": ["continue", "stop"]
    },
    "reason": {
      "type": "string"
    }
  },
  "required": ["decision", "reason"]
}
